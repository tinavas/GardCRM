###########################################################################################
#
#  STATAMIC CUSTOM ROUTES
#
###########################################################################################

routes:

  # SIMPLE ROUTE EXAMPLE
  # Visiting example.com/contact-us will...
  # load the /templates/contact.html template

  #/blog/categories : all-categories
  /member/login : member/login
  /member/register : member/register
  /member/dashboard : member/dashboard
  /member/customer-form : member/customer-form
  /member/search : member/search
  /member/update-profile : member/update-profile
  /member/forgotten-password : member/forgotten-password
  /member/reset-password : member/reset-password

  # ADVANCED ROUTE EXAMPLE
  # Visiting example.com/members/login will...
  # load the /templates/success.html template
  # and use the /layouts/system.html layout

  # /members/login:
    # template: success
    # layout: system

ignore:
  # - success
